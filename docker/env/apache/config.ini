[sadm]
env = apache

[os.pkg]

[sync]
apache.sites = ${sadm:env}/apache/sites /etc/apache2/sites-available

[apache]
conf.disable = charset
conf.enable = charset
mod.disable = status
mod.enable = status
site.disable = 000-default
site.enable = 000-default
